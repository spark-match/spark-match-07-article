% ============================================================================
% Seccion 8: Resultados y Demostracion
% TFP - Rubrica: 2 puntos
% ============================================================================
\section{Resultados y demostraci\'on}
\label{sec:results}

\subsection{Estado del prototipo}

El c\'odigo del proyecto se organiza en la organizaci\'on de GitHub
\texttt{spark-match}, con repositorios separados por dominio (datos, backend,
agente, frontend, infraestructura, DevOps y art\'iculo). A la fecha est\'an
implementados:

\begin{itemize}[noitemsep]
  \item el \emph{pipeline de datos} que produce \texttt{features.csv} (6\,208
        combinaciones carrera--instituci\'on, 554 carreras \'unicas), con las
        variables de ingreso, costo, admisi\'on y duraci\'on ya normalizadas;
  \item el \emph{etiquetado RIASEC} de las carreras mediante el LLM en Bedrock;
  \item el \emph{agente conversacional} (Deep Agent) con cinco herramientas
        operativas: evaluaci\'on de perfil RIASEC (\texttt{evaluate\_riasec\_}
        \texttt{profile}), b\'usqueda de carreras (\texttt{search\_careers}),
        c\'alculo de afinidad vocacional (\texttt{calculate\_affinity}),
        b\'usqueda de programas reales del portal MINEDU
        (\texttt{search\_programs}, sobre las 6\,208 combinaciones
        carrera--instituci\'on) y b\'usqueda web de respaldo
        (\texttt{web\_search}), servido con FastAPI/uvicorn sobre ECS Fargate,
        trazado con LangSmith y verificado por un \emph{framework de
        evaluaci\'on} con 30 casos conversacionales (\texttt{evals/}) que
        corre autom\'aticamente en cada \emph{pull request}
        (Secci\'on~\ref{sec:prompts});
  \item el \textbf{backend serverless} (TypeScript, \'unicamente el contexto
        \texttt{identity}: autenticaci\'on, usuarios y auditor\'ia; a\'un sin
        contexto de \emph{matching}) y el \textbf{frontend} (Angular 21, con
        m\'odulos funcionales reales para \emph{auth}, \emph{chat},
        \emph{assessment}, \emph{careers}, \emph{filters}, \emph{results},
        \emph{reports} y \emph{profile}, pendientes de integrarse con el
        agente desplegado);
  \item la \emph{infraestructura} en Terraform con ambientes \texttt{dev} y
        \texttt{prod}: Lambda/EventBridge del backend, el cl\'uster ECS
        Fargate + ALB + CloudFront del agente, y la base de datos RDS
        PostgreSQL compartida.
\end{itemize}

El frontend se encuentra desplegado y accesible en el ambiente \texttt{dev}
(Figura~\ref{fig:ev-frontend}), con las vistas de inicio, filtros, chat, reporte
y perfil operativas. Adicionalmente, los flujos de inicio, perfil y filtros se
capturaron como evidencia de la experiencia de usuario en el prototipo.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\linewidth]{fig-evidencia-frontend-home.png}
    \caption{Frontend (Angular) desplegado en \texttt{dev}: p\'agina de inicio de
    Spark Match con acceso a orientaci\'on, filtros, chat y reporte.}
    \label{fig:ev-frontend}
\end{figure}

\paragraph{Observabilidad con LangSmith.} La plataforma se acompa\~na de
trazas de ejecuci\'on en LangSmith, que documentan las interacciones del
agente, las llamadas a herramientas y la latencia de cada turna.
Las Figuras~\ref{fig:langsmit1} y~\ref{fig:langsmit2} muestran ejemplos de
estas trazas, agregando evidencia del monitoreo y la calidad de la conversaci\'on.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\linewidth]{langsmit1.png}
    \caption{Trazas de LangSmith: historial de prompts, herramientas invocadas y eventos de la conversaci\'on.}
    \label{fig:langsmit1}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\linewidth]{langsmit2.png}
    \caption{Trazas de LangSmith: seguimiento de la ejecuci\'on del agente y observabilidad de sus respuestas.}
    \label{fig:langsmit2}
\end{figure}

\paragraph{Alcance real del motor de scoring.} De los cinco criterios
dise\~nados (afinidad, ingreso, costo, admisi\'on, duraci\'on), \textbf{solo
la afinidad vocacional (RIASEC) se combina hoy en un puntaje}, mediante la
herramienta \texttt{calculate\_affinity} sobre un \textbf{cat\'alogo piloto de
20 carreras} (\texttt{data/careers/}, sin universidad, ingreso, costo ni
admisi\'on). Los otros cuatro criterios ya est\'an disponibles con datos
reales: el agente los consulta sobre las 6\,208 combinaciones de
\texttt{features.csv} mediante \texttt{search\_programs} --- por ejemplo, ante
la consulta de un estudiante interesado en \emph{Medicina} en Arequipa, el
agente recomienda combinaciones \textbf{carrera--universidad concretas con
costo anual, ingreso mensual de egresados, tasa de admisi\'on y duraci\'on
reales} (Figura~\ref{fig:ev-caso3}) y \textbf{razona sobre las restricciones
del estudiante}, priorizando las opciones m\'as accesibles ante un presupuesto
limitado (Figura~\ref{fig:ev-caso3b}) --- pero esta herramienta filtra y
ordena por coincidencia de campos, no calcula el puntaje ponderado que los
combine con la afinidad (el c\'odigo la documenta expl\'icitamente como
``recuperaci\'on, no scoring''). El backend serverless, por su parte, solo
implementa el contexto \texttt{identity}; no existe a\'un un contexto de
\emph{matching}. En consecuencia, el ranking multicriterio y el ejemplo
num\'erico de la Secci\'on~\ref{sec:implementation} describen el dise\~no
objetivo del sistema, no un resultado ya medido end-to-end. La integraci\'on
de \texttt{search\_programs} es reciente y a\'un en estabilizaci\'on: algunos
valores de costo o ingreso figuran como \emph{estimados} por imputaci\'on y el
reporte descargable sigue pendiente. El trabajo pendiente para cerrar esta
brecha se detalla en \texttt{DOCS/issues/issues-v1/}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{fig-evidencia-caso3-explicacion.png}
    \caption{Ejecuci\'on real (integraci\'on de datos): el agente consulta el
    cat\'alogo y recomienda carrera--universidad con duraci\'on, costo anual,
    ingreso de egresados y tasa de admisi\'on reales.}
    \label{fig:ev-caso3}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{fig-evidencia-caso3-medicina.png}
    \caption{Ejecuci\'on real: ante un presupuesto limitado, el agente prioriza
    las opciones m\'as accesibles con sus costos e inversi\'on total.}
    \label{fig:ev-caso3b}
\end{figure}

\paragraph{Actualizaci\'on: integraci\'on del cat\'alogo con datos completos.}
Con posterioridad a la redacci\'on del p\'arrafo anterior se integr\'o la
consulta del agente al cat\'alogo de carreras con sus datos reales. En una
ejecuci\'on en \texttt{dev}, ante la consulta de un estudiante interesado en
\emph{Medicina} en Arequipa, el agente ahora recomienda combinaciones
\textbf{carrera--universidad concretas con costo anual, ingreso mensual de
egresados, tasa de admisi\'on y duraci\'on reales}
(Figura~\ref{fig:ev-caso3}), y \textbf{razona sobre las restricciones del
estudiante}: ante un presupuesto limitado prioriza las opciones m\'as
accesibles (Figura~\ref{fig:ev-caso3b}). Esto cierra en buena parte la brecha
del Criterio~4. La integraci\'on es reciente y a\'un en estabilizaci\'on:
algunos valores de costo o ingreso figuran como \emph{estimados} por imputaci\'on
y el reporte descargable sigue pendiente.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{fig-evidencia-caso3-explicacion.png}
    \caption{Ejecuci\'on real (integraci\'on de datos): el agente consulta el
    cat\'alogo y recomienda carrera--universidad con duraci\'on, costo anual,
    ingreso de egresados y tasa de admisi\'on reales.}
    \label{fig:ev-caso3}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{fig-evidencia-caso3-medicina.png}
    \caption{Ejecuci\'on real: ante un presupuesto limitado, el agente prioriza
    las opciones m\'as accesibles con sus costos e inversi\'on total.}
    \label{fig:ev-caso3b}
\end{figure}

\paragraph{Actualizaci\'on: integraci\'on del cat\'alogo con datos completos.}
Con posterioridad a la redacci\'on del p\'arrafo anterior se integr\'o la
consulta del agente al cat\'alogo de carreras con sus datos reales. En una
ejecuci\'on en \texttt{dev}, ante la consulta de un estudiante interesado en
\emph{Medicina} en Arequipa, el agente ahora recomienda combinaciones
\textbf{carrera--universidad concretas con costo anual, ingreso mensual de
egresados, tasa de admisi\'on y duraci\'on reales}
(Figura~\ref{fig:ev-caso3}), y \textbf{razona sobre las restricciones del
estudiante}: ante un presupuesto limitado prioriza las opciones m\'as
accesibles (Figura~\ref{fig:ev-caso3b}). Esto cierra en buena parte la brecha
del Criterio~4. La integraci\'on es reciente y a\'un en estabilizaci\'on:
algunos valores de costo o ingreso figuran como \emph{estimados} por imputaci\'on
y el reporte descargable sigue pendiente.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{fig-evidencia-caso3-explicacion.png}
    \caption{Ejecuci\'on real (integraci\'on de datos): el agente consulta el
    cat\'alogo y recomienda carrera--universidad con duraci\'on, costo anual,
    ingreso de egresados y tasa de admisi\'on reales.}
    \label{fig:ev-caso3}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{fig-evidencia-caso3-medicina.png}
    \caption{Ejecuci\'on real: ante un presupuesto limitado, el agente prioriza
    las opciones m\'as accesibles con sus costos e inversi\'on total.}
    \label{fig:ev-caso3b}
\end{figure}

\subsection{Ejemplo de salida (caso can\'onico, dise\~no objetivo)}

El siguiente ejemplo ilustra el comportamiento \textbf{objetivo} del sistema
una vez integrado el motor de scoring completo; \textbf{no} es una captura de
una ejecuci\'on real. Es importante precisar qu\'e parte es ya real y qu\'e
parte sigue siendo dise\~no: el filtrado por regi\'on y presupuesto sobre
datos reales \textbf{ya existe} --- la herramienta \texttt{search\_programs}
lo aplica hoy sobre las 6\,208 combinaciones (ver el caso de Medicina en
Arequipa m\'as arriba) ---, pero los \textbf{pesos num\'ericos} de
ingreso/costo/admisi\'on/duraci\'on que combinan esos criterios en un \'unico
puntaje ponderado a\'un no existen en el agente (Secci\'on~\ref{sec:prompts}).
Por eso el JSON de preferencias de abajo, con sus cinco pesos expl\'icitos,
sigue siendo dise\~no objetivo y no una extracci\'on real.

\textbf{Caso:} estudiante de Cusco, presupuesto m\'aximo S/600/mes, prioridad en
salario y cercan\'ia geogr\'afica.

\textbf{Mensaje del usuario:} \textit{``Vivo en Cusco y mi presupuesto m\'aximo es
600 soles al mes. Me interesa algo con buenos sueldos pero tambi\'en quiero
quedarme cerca de casa''.}

\textbf{Preferencias extra\'idas por el LLM:}
\begin{lstlisting}[style=jsonstyle, caption={JSON de preferencias extraidas}, label={lst:json-prefs}]
{
  "region": "Cusco",
  "presupuesto_max": 600,
  "peso_afinidad": 0.15,
  "peso_ingreso": 0.45,
  "peso_costo": 0.30,
  "peso_admision": 0.05,
  "peso_duracion": 0.05
}
\end{lstlisting}

El motor de scoring aplica los filtros duros (regi\'on Cusco, presupuesto) y
ordena las combinaciones carrera--universidad por su puntaje, generando un ranking
Top-N. Finalmente, el agente redacta una justificaci\'on en lenguaje natural que
cita los datos que sustentan cada recomendaci\'on (ingreso promedio, costo,
afinidad). La subsecci\'on siguiente muestra una \textbf{ejecuci\'on real} del
agente sobre el alcance ya integrado.

\subsection{Ejemplo de salida real (ejecuci\'on del agente en \texttt{dev})}
\label{sub:evidencia-real}

A diferencia del ejemplo objetivo anterior, esta subsecci\'on documenta una
\textbf{ejecuci\'on real} del agente desplegado en el ambiente \texttt{dev}
(\url{https://d3sx4v4l1enpvk.cloudfront.net}), capturada durante las pruebas de
QA. \textbf{Nota temporal:} esta captura es anterior a la integraci\'on de
\texttt{search\_programs} descrita arriba (Figuras~\ref{fig:ev-caso3}
y~\ref{fig:ev-caso3b}) y ejercita un flujo distinto y todav\'ia independiente:
la \textbf{entrevista RIASEC escalonada} y el \textbf{c\'alculo de afinidad}
sobre el cat\'alogo piloto, sin pasar por \texttt{search\_programs}. Ambos
flujos --- afinidad sobre el cat\'alogo piloto y b\'usqueda de datos reales
por regi\'on/presupuesto --- son hoy capacidades reales pero \textbf{separadas}
del agente; combinarlas en un \'unico ranking es, precisamente, la funci\'on
de \emph{scoring} pendiente descrita en la Secci\'on~\ref{sec:implementation}.

\paragraph{Caso A --- perfil t\'ecnico.} El estudiante completa la entrevista
situacional; el agente infiere el c\'odigo \textbf{ICR} (Investigativo~9,
Convencional~8, Realista~6) en tres o cuatro turnos y lo confirma antes de
cerrar. Al solicitar recomendaciones, el subagente \texttt{matching} calcula la
afinidad y devuelve un ranking encabezado por \textbf{Ciencia de Datos (100\%)}
y \textbf{Ciencias de la Computaci\'on (75\%)}. Ante la pregunta ``\textit{?`por
qu\'e esa carrera?}'', el agente \textbf{explica el c\'alculo} (comparaci\'on
posicional de los c\'odigos RIASEC), lo que evidencia la trazabilidad de la
recomendaci\'on.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\linewidth]{fig-evidencia-ranking-icr.png}
    \caption{Ejecuci\'on real: ranking para el perfil ICR, encabezado por
    Ciencia de Datos (afinidad 100\%). El agente opera sobre el cat\'alogo
    piloto y solo aplica el criterio de afinidad RIASEC.}
    \label{fig:ev-icr}
\end{figure}

\paragraph{Explicabilidad de la recomendaci\'on.} Ante la pregunta del estudiante
por el fundamento del ranking, el agente detalla c\'omo se calcula la afinidad:
descompone el perfil RIASEC inferido, muestra el c\'odigo de cada carrera del
cat\'alogo y explica la comparaci\'on posicional que produce el porcentaje de
afinidad (Figuras~\ref{fig:ev-expl-1} y~\ref{fig:ev-expl-2}). Esto materializa el
principio de dise\~no del sistema: el LLM \textbf{interpreta y explica}, pero la
recomendaci\'on se sustenta en un c\'alculo transparente y reproducible, no en una
respuesta opaca del modelo.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\linewidth]{fig-evidencia-explicacion.png}
    \caption{Ejecuci\'on real: el agente explica el fundamento de la
    recomendaci\'on (perfil RIASEC del estudiante y de cada carrera).}
    \label{fig:ev-expl-1}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\linewidth]{fig-evidencia-explicacion-2.png}
    \caption{Ejecuci\'on real (cont.): c\'alculo de afinidad por comparaci\'on
    posicional de c\'odigos RIASEC y validaci\'on con los intereses declarados.}
    \label{fig:ev-expl-2}
\end{figure}

\paragraph{Caso B --- perfil social (usuario distinto).} Un segundo estudiante,
en una \textbf{cuenta diferente}, completa la entrevista; el agente infiere el
c\'odigo \textbf{SAE} (Social~9, Art\'istico~8, Emprendedor~7), coherente y
opuesto al del Caso~A. El ranking resultante lo encabeza
\textbf{Educaci\'on/Pedagog\'ia (100\%)}, seguida de Turismo, Periodismo,
Marketing y Psicolog\'ia --- carreras sociales, \textbf{sin ninguna filtraci\'on}
del perfil t\'ecnico del Caso~A. Esto evidencia el \textbf{aislamiento de la
memoria por usuario} (\texttt{langmem} bajo el espacio de nombres del JWT).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\linewidth]{fig-evidencia-ranking-sae.png}
    \caption{Ejecuci\'on real: ranking para el perfil SAE (usuario distinto),
    encabezado por Educaci\'on/Pedagog\'ia (100\%). No hay filtraci\'on del
    perfil del Caso~A, lo que confirma el aislamiento por usuario.}
    \label{fig:ev-sae}
\end{figure}

\paragraph{Qu\'e validan estas ejecuciones.} Las capturas confirman que
funcionan \textbf{la entrevista RIASEC escalonada}, la \textbf{inferencia
coherente} del perfil en dos casos opuestos, la \textbf{explicabilidad} de la
recomendaci\'on y el \textbf{aislamiento por usuario}. Como se se\~nal\'o
arriba, este flujo opera \'unicamente sobre afinidad RIASEC; lo pendiente
sigue siendo la \textbf{funci\'on de scoring} que la combine con los datos
reales de \texttt{search\_programs} en un \'unico ranking ponderado
(Secci\'on~\ref{sec:implementation}).

\subsection{Evidencia funcional}

La demostraci\'on final mostrar\'a el flujo de extremo a extremo: el estudiante
conversa con el orientador, el agente construye su perfil y prioridades, el motor
de scoring genera el ranking Top-N sobre datos reales y el agente entrega las
recomendaciones explicadas. La evidencia incluir\'a:

\begin{itemize}[noitemsep]
  \item \textbf{Repositorios:} \url{https://github.com/spark-match}.
  \item \textbf{Demo / video:} \url{https://youtu.be/c_wMR6HVe1M}.
  \item \textbf{Ambiente \texttt{dev} desplegado:}
        \url{https://d3sx4v4l1enpvk.cloudfront.net} (ver Secci\'on~\ref{sub:evidencia-real}).
  \item \textbf{Video de exposici\'on:} Secci\'on~12.
  \item \textbf{Capturas:} ejecuciones reales del agente
        (Figuras~\ref{fig:ev-icr} y~\ref{fig:ev-sae}), ranking Top-N y trazas de
        observabilidad en LangSmith.
\end{itemize}
